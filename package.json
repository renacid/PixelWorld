{
  "name": "pixel-world", "version": "0.1.0", "private": true, "type": "module",
  "engines": { "node": ">=22.18.0" },
  "scripts": { "dev": "vite --host 0.0.0.0 --configLoader native", "build": "tsc --noEmit && vite build --configLoader native", "preview": "vite preview --host 0.0.0.0 --configLoader native", "test": "vitest run --configLoader native" },
  "devDependencies": { "typescript": "~5.9.3", "vite": "^7.1.7", "vitest": "^3.2.4" }
}
