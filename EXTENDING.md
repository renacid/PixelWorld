# 拡張ガイド

宝箱4種類・敵ごとのドロップ・隠し罠6種類の設定手順は [LOOT_AND_TRAPS.md](LOOT_AND_TRAPS.md) を参照してください。

## 床・壁・ドット絵を増やす

`src/data/terrain.ts` に新しい数値IDを追加します。保存データがIDを記録するため、既存のIDを別用途へ変更しないでください。

```ts
6: {
  name: '石畳', solid: false, blocksSight: false, color: '#c6c4b4',
  pixels: ['....', '.ss.', '....', 's..s'],
  palette: { s: '#999b91' },
},
```

- `solid`: trueなら移動できません。
- `blocksSight`: trueなら敵の索敵とプレイヤーの視界を遮ります。水面のような「通れないが見通せる」地形も作れます。
- `pixels`: 各行を同じ文字数にしたドット配列です。パレットにない文字は背景色になります。16×16や32×32を推奨します。
- PNGを使う場合は `public/tiles/stone.png` に配置し、`image: 'tiles/stone.png'` を指定します。先頭の `/` は不要です。GitHub Pagesのサブパスでも動作します。
- 地形は通常画面では薄くしません。ミニマップへの記録と敵の非表示には従来の視界を使用します。

## 森1〜5を作る

1. `src/stages/index.ts` の `STAGES` へID6〜10を順番に追加します。ステージIDは1から連番にしてください。名前は「森1」〜「森5」で自由に設定できます。
2. `src/stages/forest.ts` の現行レイアウトを編集するか、MAP_CREATION.mdの文字マップ例を使います。旧サンプルは削除済みです。
3. `CUSTOM_LAYOUTS[6] = 作成したレイアウト;` のように登録します。未登録のステージは既存の自動生成になります。
4. ステージの `width` / `height` と、`rows` の列数・行数を合わせます。文字の意味を `legend` に設定します。
5. `spawn` に開始位置、`objects` に宝箱・出口、`enemies` に敵、`fields` に設置効果を指定します。座標は左上がx=0,y=0です。

```ts
// STAGES配列の末尾に追加する例
{ id: 6, name: '森1', subtitle: '木漏れ日の道', description: '新しい森の入口。',
  objective: '古代の宝箱を1個回収し、出口へ', vision: 5,
  width: 11, height: 11, enemyCount: 1, goal: 'treasure', requiredChests: 1 },
```

解放・保存・次のステージへの遷移はSTAGESの件数に追従します。森1〜5そのものの完成コンテンツはまだ追加していません。開始位置・出口までの到達可能性は実際にプレイして確認してください。地形サイズ不一致、未定義文字、壁との重なりは生成時にエラーで通知します。

## スキル・敵・数値の調整

- `src/data/skills.ts`: 消費MP、CT、属性、形状と説明。スキルIDは`src/game/types.ts`、表示アイコンは`src/main.ts`にも追加します。新しい効果は`GameSession.cast`と`SkillResolver.previewSkill`へ実装します。
- 初期宝箱は`MapState.ts`の`start-attack`です。`skillId`に加えて`skillIds`で複数スキルを同時取得できます。
- ワープ候補は周囲9×9、隣接8マスを除く空き床です。壁・敵・味方・宝箱・道具・出口は除外します。候補がない場合はMPも行動も消費しません。
- ダメージは`GameSession.damage`で切り捨てます。会心・レベル補正後に整数化し、その実ダメージを属性反応の元にします。1未満は0です。
- `src/data/enemies.ts`: 占有は1×1・2×2・3×3のみ。`chaseMoveLimit`（初期12）回の連続追跡後は1行動スキップし、`chaseRecoveryChance`（初期0.3）の抽選で回復。失敗なら`chaseSkipLeft`を1に保ち次の行動もスキップ・再抽選します。スキップ中に全対象が敵固有の索敵距離外に出た場合は敵視解除。攻撃すると連続追跡回数がリセットされます。
- ウルフは1×1マスです。4方向のドット絵は`SpriteAtlas.ts`で編集します。旧セーブの2マスウルフも再開時に1マスへ縮小します。
- `src/game/TurnManager.ts`: MP回復周期。敵ドロップは`enemies.ts`の`drops`で個別設定します。初期道具は空、追加の補給宝箱は25%の冒険で生成します。

## 表示と検証

### 敵スキルの拡張

#### ゴブリンの投石（2026-09-16追加）

- ゴブリンの既存設定（HP15＋ステージ×2・攻撃力3・索敵4）を維持し、4方向のドット絵を追加しました。
- `stoneThrow`：自然属性、上下左右1〜3マス、MP1、発動率25%、攻撃力×30〜40%。壁と手前のキャラクターに遮られます。対象はプレイヤーまたは召喚味方です。
- 初期MP5。回復はないので最大5回発動します。自然属性は現時点では付着・反応を持ちません。
- 現在の攻撃力3では切り捨てにより0〜1ダメージです。ダメージも確認したい場合はゴブリンの攻撃力を変更してください。例：10なら3ダメージになります。
- 平原5の通常敵4枠をゴブリンに変更しました。ボスは維持しています。既存セーブのマップは変更せず、新しく平原5を開始したときに生成します。

#### 敵設定の集約（実装済み）

敵の能力・索敵・追跡・MP・スキル・描画設定は `src/data/enemies.ts` の `ENEMIES` に集約しました。

新しい敵は、同ファイル内の一覧へ次のように追加します。

```ts
eliteGoblin: define({
  name: 'ゴブリン隊長', hp: 25, hpPerStage: 2, attack: 6,
  size: 1, detectionRange: 5, pattern: 'guard',
  mp: 10, skills: ['stoneThrow'], sprite: 'goblin',
}),
```

- HPは `hp + ステージID × hpPerStage` です。
- `size` は一辺のマス数（1・2・3）。占有1・4・9マスに対応します。
- `sprite` は使用する絵のID。既存の絵を共有すれば描画プログラムの変更は不要です。
- `renderScale` は16×16絵の拡大率（標準2、既存ゴーレム3、ボス5）。占有サイズとは別設定です。
- `bob` は上下に浮く演出、`idleStep` は待機時の足踏みです。
- 省略値は同ファイルの `define` にまとまっています。新しい敵IDは型に自動追加されるので `types.ts` の編集は不要です。

出現させるステージの `src/stages/index.ts` にある `enemySpawns` へ追加します。

```ts
{ kind: 'eliteGoblin', count: 3 },
// 固定配置の場合（同じ座標に複数は指定できません）
{ kind: 'eliteGoblin', count: 1, position: { x: 8, y: 9 } },
```

出現表の順番に生成します。`enemySpawns` がある場合、総数は各countの合計になり、旧 `enemyCount` は使いません。未指定の旧ステージだけenemyCount体のスライムを生成します。手作り地形は引き続き `layouts.ts` のenemiesにkindと座標を登録します。

独自の絵が必要なら `enemies.ts` の `SpriteId` と `render/SpriteAtlas.ts` に絵を追加し、定義のspriteで指定します。新しいスキル効果は `enemySkills.ts` と `EnemySkillResolver.ts` で追加します。既存の絵・スキルを使う敵なら、編集は「敵定義」と「出現表」の2箇所だけです。

旧ENEMY_LOADOUTSは廃止しました。セーブ内のHP・MP・行動状態は再開時に維持し、未設定のMP・スキルだけ共通定義から補完します。能力・サイズ・出現表の変更は新しい冒険で確認してください。描画設定は既存セーブにも反映します。

- 敵への命中時は即座に敵視へ移行し、攻撃者の位置を追跡先に記録します。追跡疲労のスキップは解除します。召喚味方の攻撃・属性反応・切り捨てによる0ダメージでも反応します。処理は `GameSession.damage` にまとめています。

- `src/data/enemySkills.ts` の `ENEMY_SKILLS` に名前・MP消費・発動率・距離範囲・対象・遮蔽物条件を定義します。敵ごとの初期MPとスキルIDは `enemies.ts` のmp・skillsで設定します。
- `src/skills/EnemySkillResolver.ts` が条件判定→移動先検証→抽選→MP消費→効果実行を担当します。新しい効果は `EnemySkillDefinition.effect` の型と `prepare`・実行処理を拡張します。同じ接近攻撃の数値違いはデータの追加だけで作れます。
- 通常攻撃・移動より前に抽選します。MP不足、範囲外、移動先が塞がっている場合は抽選せず、抽選失敗時は通常行動に戻ります。成功時は同じ行動内に通常攻撃を追加しません。追跡疲労のスキップ中は発動しません。
- ウルフの「飛びかかり」：プレイヤーが上下左右のちょうど2マス先にいて、間のマスへ移動可能な場合に20%で発動。1マス接近し、自分の攻撃力×30〜40%を切り捨てて与えます。現状のウルフは攻撃力3なので0〜1ダメージです。
- ウルフの初期MPは5、消費も5です。MP回復は未設定。MPはセーブされ、旧セーブでは未設定の場合だけ初期値を補います。
- 描画は地形の後にキャラクターを描きます。大型キャラの絵をマス単位で切り抜かず、一部の占有セルが見えたら一体を描画します。完全に視界外の敵は非表示です。

- `GameCanvas.ts`: 320×320のCanvasに11×11セルを描画し、外周は半マスずつ見切れます。画面の縦横比を崩すと見切れ量が変わるため、CSS側も正方形を維持します。
- `TurnAnimation.ts`: ルール上の座標を変えず、移動・攻撃の表示を補間します。ダメージポップアップは次の行動で消去せず、表示期限まで維持します。
- `main.ts`: マップ左下のイベント表示は約3秒で消え、ログボタンから最大100件を確認できます。スキルは再タップで解除し、発動可能な間だけマップ内下部にテキスト幅の半透明確定ボタンを表示します。
- `pop.css`: 明るい配色、コンパクトUI、ブロック形状のドラッグ表示。バッグ変更はターンを使わず、右上の×で保存して閉じます。
- `audio/SoundManager.ts`: Web Audio効果音と有効・無効設定。
- `npm test`で回帰テスト、`npm run build`で型検査と静的ビルドを実行します。
- 開発用の`/dev/preview.html?fixture=1`は専用セーブで演出を検証できます。実際のセーブを上書きせず、公開ビルドには含まれません。

## アイスストーンと指定地点型スキル

- 定義は `src/data/skills.ts` の `icestone`。2×4、MP8、CT10、攻撃力50%の氷攻撃です。
- `target: 'pointArea'` はマップをタップして地点を選択する型です。`range` は選択範囲の半径、`areaRadius` は着弾範囲の半径です。
- `rangeUpgrade: { level: 3, range: 3 }` により実効Lv.3で選択範囲が5×5から7×7になります。壁越しにも落下し、攻撃範囲内の敵に1回ずつ命中します。
- 新しいスキルをこの型にすると選択UI・コマンドのtarget・範囲検証を再利用できます。絵と音は別途設定してください。
- 炎＋氷の融激は `dealAttributeHit` で、後から与えたダメージをシード乱数による1.5〜2倍にして最後に切り捨てます。両属性を消費し、後続ヒットは通常の付着に戻ります。
- MP回復は `mpRecoveryActions` を保存します。スキル使用では増えず、移動・待機で5に達したらMP3回復して0へ戻ります。旧セーブにこの項目がなければ0から始めます。

## スマホから開発画面を開く

1. PCとスマホを同じWi-Fiに接続します。
2. PCで `npm run dev` を実行し、ターミナルのNetwork URL（例：`http://192.168.0.11:5173/`）をスマホのSafari/Chromeで開きます。IPとポートは実際に表示された値を使ってください。
3. 専用の演出確認画面は、そのURLの末尾を `/dev/preview.html?fixture=1` にします。「続きから」でアイスストーンを含むスキルを所持した状態から始められます。バッグに入らないスキルはバッグ外に保管されます。
4. `?fixture=1` は開くたびに開発用データを初期化します。続きから再開するときはクエリを外してください。通常プレイとは保存先が別です。

スマホのlocalhostはスマホ自身を指すため使いません。接続できなければ、PC側のファイアウォールでNode.jsのプライベートネットワークへの接続許可を確認してください。開発サーバーを閉じるとアクセスできなくなります。

森の手作りマップ・ドット絵・敵・宝箱・罠の配置例は [MAP_CREATION.md](MAP_CREATION.md) を参照してください。

## 森・昼夜・召喚精霊の追加設定

- 森1・2の名前と25×40サイズは維持し、`src/stages/forest.ts` で曲がった通路と突き出した入口・出口を生成しています。`src/stages/layouts.ts` の `CUSTOM_LAYOUTS[6]` / `[7]` に登録済みです。クリア条件は表示内容に合わせて出口到達へ変更しました。
- 森の配置を変えるには `path`（曲がり角）、`gates`（トレントの守る通路）、`camps`（通常敵）、`objects`（宝箱・出口）、`trapPlacements` を編集します。完全手描きの `rows` に差し替えることもできます。地形ID6は森の外側に使用済みです。
- トレントは `src/data/enemies.ts` の `treant`。HP35、攻撃力5、MP20、移動なし、回復薬70%。`innateAttribute` を行動開始時に再付着します。
- `skillChances` はモンスターごとのスキル発動率上書きです。投石50%、森の加護20%。支援を先に抽選し、成功した行動では攻撃しません。投石50%は支援が発動せず射程条件を満たした場合の抽選率です。
- `forestBlessing` は周囲5×5の自分以外の味方モンスターを強化します。MP3、攻撃力1.25倍、索敵範囲+1、10ターン。同じバフは積み重ねず残り時間を更新します。効果は `ActorStats.ts` で計算し、基礎ステータスを書き換えません。
- 敵の会心率は `criticalRate: .05`、会心ダメージは `criticalMultiplier: 1.5` が共通初期値です。各敵で上書きできます。
- 自然属性は10ターン付着します。旧earthは読み込み時にnatureへ移行します。自然付着中の対象への**無属性neutral**攻撃だけ会心率が2倍です。物理physicalとは別です。これによる付着消費や残り時間の更新はありません。
- 精霊の帰還と追従は `src/ai/SummonAI.ts`。旅人中心9×9の外から、空き床へ最優先で帰還します。精霊は追跡疲労の対象外。索敵は全方向の距離と視線で判定し、見える敵を優先します。精霊をタップすると状態を確認できます（地点選択スキルの照準中は着弾点の選択を優先）。
- 昼夜は `src/game/DayCycle.ts`。昼0〜79、夕80〜99、夜100以降。移動・待機・スキルで進み、道具では進みません。道具は敵行動・CT更新・MP回復カウントも進めません。
- 夜は視野上限4マス。ゲージが睡眠ボタンになるのは、生存敵に敵視中の個体がいない場合です。睡眠はHP・MP全回復、朝に戻り、期限付き属性・バフ・CTを解消します。撃破済み敵を最大3体、旅人の周囲9×9より外の空き床へ復活させます。場所がない敵は復活しません。
- ステージの `sleepRespawnCount` で復活数を指定できます。省略時は3。既存セーブで記録がない過去の撃破は復活対象にできず、更新後の撃破から記録します。
- ログは攻撃名と対象別の合計ダメージをまとめます。会心を含む複数ヒットは「合計会心…」と表示し、合計値にはその攻撃に伴う属性反応ダメージも含みます。
- 最新5件は5秒表示後に2秒でフェード。6件以上になった際は、古い行から2秒でフェードします。履歴は100件のままです。

### スマホで白画面になったとき

通常画面と `/dev/preview.html` に読み込みエラー表示を追加しました。同じWi-FiでPCのNetwork URLを再読み込みし、15秒待つと、起動に失敗した場合は確認用のメッセージが出ます。その文面とURL・iOSバージョンが切り分けに役立ちます。現在、スマホ固有の原因は未特定です。

## 千里眼・劣化・地砕き・強化宝石

### 表示と視野

- カメラ復帰ボタンは左上の目的表示の下へ配置しました。ログは下から短くスライドして出現し、新しい行ほど下に並びます。
- プレイヤーから見える敵・宝箱は範囲だけで判定し、木や壁による隠れを無効にしました。敵AIの索敵と攻撃の遮蔽物判定は維持します。
- 千里眼薬（小）は取得時に視野を2加算し、何個でも累積します。道具枠へは入らず即時適用です。旧セーブで道具枠に入っている分も使用できます。取得済み旧セーブの視野+2は引き継ぎます。
- カメラを動かすだけでは未探索域を記録しません。まだ視野に入ったことのない地形は黒です。薬で増えた範囲では敵・宝箱も見えます。夜も追加された視野分を維持します。
- 睡眠後の敵復活ログは削除。復活処理そのものは継続します。

### スキル劣化

`src/skills/SkillWear.ts` に独立しています。スキルごとに使用回数・追加MPを保存し、6回目以降の各成功発動時に20%の抽選をします。成立すると基礎MPの10%（切り上げ、最低1）を追加し、使用カウントを0に戻します。加算は累積し、抽選が成立した次の発動から新しい消費MPになります。

表示例：`MP9（+1）`。同じスキルを再取得したとき、使用カウントと追加MPの両方をリセットします。睡眠・バッグ配置替えではリセットしません。

### 地砕き

- ID `groundbreak`。自然属性、MP7、CT5、T字4マス。
- 現在地に罠を設置。同時2個。物・未発動の罠・設置効果がある場所は不可。
- 敵が移動して踏むと1回だけ自然の固定7ダメージと自然付着。設置時の実効Lv.3以上で12ダメージ。会心・攻撃力倍率は適用しません。
- 設置罠は `mapState.playerTraps` に保存します。味方・旅人の移動では発動せず、敵の踏破で消えます。
- 宝箱の抽選候補と森2の固定宝箱に追加しました。

### 強化宝石

- 効果一覧は `src/data/gems.ts` の `GEM_REWARDS`。指定の10種類を登録済みです。
- 宝石を拾うと異なる候補3つをシード乱数で提示し、1つだけ選びます。候補は保存し、再開しても変わりません。選択中はターンが進みません。
- 最大HP・MPの強化では現在値も同じだけ加算します。スキル取得は既存の重複レベルアップ・劣化回復に対応します。
- 強化はその冒険中だけ有効です。新しい冒険では初期化されます。

### 現在の森マップ

旧forest06とFOREST_EXAMPLEは削除し、`forest.ts` に一本化しました。

| 設定 | 森1 | 森2 |
| --- | ---: | ---: |
| サイズ | 35×54 | 39×60 |
| 固定トレント | 3 | 3 |
| ランダム敵 | 14 | 20 |
| 固定宝箱 | 5 | 5 |
| ランダム宝箱 | 7 | 9 |
| 罠 | 16 | 22 |
| 強化宝石 | 3 | 4 |

固定宝箱のうち3個は行き止まりの奥にあります。ランダム配置の設定は `StageLayout.randomEnemies` / `randomChests` / `gemCount` と `trapPlacements`。敵・箱・宝石・罠が互いに重ならない床を選びます。中断後は保存済みの配置を維持します。

### 会心初期値の編集場所

`src/data/enemies.ts` の `define()` の初期値が敵共通（`criticalRate: .05`, `criticalMultiplier: 1.5`）。各敵の定義内で上書きできます。同ファイルの `SUPPORT_ACTORS.player` が旅人の初期値（`.15`, `1.5`）です。`createPlayer()` に別の固定値は置かず、この定義を参照するようにしました。

## 複数階層・夜の復活の設定（最新）

各ステージの総階層数は固定です。平原1〜2は1層、平原3〜5は2層、森1〜2は3層です。出口条件を満たすと次の層へ進み、最終層を抜けるとステージクリアです。HP・MP・成長・スキル・劣化・道具・精霊・昼夜カウントは引き継ぎます。階層の地形・敵・罠・探索記録・宝箱回収数は切り替わります。戻る階段はありません。再開時も現在の固定層数を使用します。ただし現在層より少ない設定に変更した場合、現在層を最終層として扱います。

`src/stages/index.ts` の各ステージにある `dungeon` で調整します。

```ts
dungeon: {
  floors: 3,                 // このダンジョンの固定階層数
  enemyScaling: { everyFloors: 3, multiplier: 1.2 }, // 森の設定
  gemCount: 1,              // 各階の宝石。0〜1
  extraPassages: 16,        // 床に接した壁を開ける回数。0で形状の変化なし
  enemyVariance: 1,         // ランダム敵の各グループの基準数±1
  nightRevival: { min: 1, max: 2 }, // 夜に復活する撃破済み敵の数
  overrides: {
    2: { gemCount: 0, extraPassages: 24, enemyVariance: 2 },
  },
},
```

- 合成処理は `src/stages/DungeonRules.ts`。固定配置の敵数は変えず、ランダム配置グループの数だけ変動します。
- 既存の通路に接続した壁を床へ変えるため、元の通路を閉じずに枝道や輪郭が変わります。固定宝箱と出入口は維持します。
- 宝石は固定配置分も含め1階につき最大1個です。手作りマップの `StageLayout.gemCount` でも設定可能。階層別 `overrides` が最優先です。
- 夜の復活は探索済みの壁に隣接する空き床から出現します。壁を通行不可のまま保つため、壁セル自体には置きません。
- その階で倒した敵から抽選します。撃破済み敵がいない場合、または置ける床がない場合は復活できません。最低数に達していなければ、その夜の後続ターンで再試行します。
- `nightRevival.min: 0` で夜の復活を無効化できます。睡眠後の復活数は既存の `sleepRespawnCount`（省略時3）で別に調整します。
- 現在層、夜の復活済み数を保存します。総階層数はステージの固定設定を参照します。

## スキル・バッグ・砂時計の追加変更

- 無属性 `neutral` と物理 `physical`（アタック）はMP劣化対象外。既に保存された追加MPも消費・表示へ適用しません。
- スキル欄にCTを常時表示します。待機中は残りCT、使用可能時は基本CTです。発動後にCT・MP等で再発動不可なら選択を自動解除します。
- `hourglass`（砂時計（小））は消費アイテム。使用時に再使用待ちのスキルを選び、残りCTを10減らします（最低0）。ターンは経過せず、対象がなければ消費しません。宝箱の一般道具抽選へ追加済みです。
- バッグは盤面を固定し、下の一覧のみスクロール。タッチで一覧の文字部分を上下に動かすとスクロール、アイコンをつかむと配置ドラッグになります。
- 赤い×は盤面横へ移動。未配置スキルはバッグ外に保管され、閉じても破棄しません。破棄する操作がないため、破棄警告は発生しません。
- バフ中の敵・精霊には「↑強化中」を表示します。画面外だけで起きた森の加護などの支援ログは通知しません。バフ処理自体は継続します。

### 階層ごとの地形と敵の強化

- `src/stages/index.ts` の `dungeon.floors` が固定の階層数です。
- `enemyScaling` はHP・最大HP・MP・最大MP・攻撃力に適用し、小数点以下を切り捨てます。会心率・索敵範囲は変えません。`everyFloors: 3, multiplier: 1.2` は第1〜2層が1倍、第3〜5層が1.2倍、第6〜8層が1.44倍です。省略時は1倍です。
- 夜・睡眠による復活、罠で召喚された敵にも現在層の倍率を適用します。すでに保存されている敵のステータスは再計算しません。
- 第1層は既存マップ、第2層以降は `src/stages/upperFloors.ts` を使用します。偶数層は環状路、奇数層は蛇行路と部屋です。初期スキル保証の宝箱は第1層だけです。
- 開始地点もレイアウトの `spawn` に従います。階層移動時に冒険ログを表示します。
- 新しい地形は次の階層生成・新規冒険から反映します。中断中の地形は保持します。

## 経験値・不定形バッグ（最新）

- 新規冒険はLv.1・経験値0・4×4バッグ。最大Lv.20、レベルごとに上下左右に接する未解放セルを1個抽選し、最大35セルになります。階層間は引き継ぎ、別の冒険ではリセットします。旧セーブの5×5は配置保護のため維持します。
- 成長率は \src/game/Progression.ts\ の PROGRESSION で設定します。必要経験値は10→12→15→18→22→27…（前回値の20%を切り上げて加算）。超過分は持ち越し、最大レベルでは加算を止めます。
- 最大HP・MPは直前の最大値の10%を切り上げて加算し、その増加分だけ回復します。初期値は enemies.ts の SUPPORT_ACTORS.player を参照するため、固定の成長テーブルはありません。宝石による最大値増加も成長計算に含みます。
- 敵の experience が撃破経験値です。スライム2、狼3、ゴブリン4、トレント5、守護岩8、ボス15。召喚精霊・罠による撃破も経験値を得ます。
- バッグの有効セルは SaveData.bagCells、進行はplayerLevelとexperienceに保存します。配置とドラッグは同じセル集合で判定します。
- 未配置スキルは閉じる際に確認して消滅し、レベル・劣化・CTの記録も消えます。新規スキルは一覧先頭、new表示、説明が開いた状態になります。説明をタップすると開閉し、左のアイコンでドラッグできます。
- MPはスキル以外の5行動ごとに最大MPの1/10（切り捨て）回復します。道具は引き続き行動数に含めません。

## 精霊との交換・夜間の連続復活

- 精霊のいるマスへ移動すると旅人と場所を交換します。交換は旅人の移動1行動で、精霊は続く味方フェーズで通常どおり行動します。精霊のステータス画面から確認付きで消滅させることもできます（ターン消費なし）。
- 夜間の復活設定は src/game/DayCycle.ts。カウント100の夜は経験値50%、150の深夜以降は25%。以降180、210…で追加復活を行います。復活数は各階のnightRevival.min/maxを利用します。
- 復活対象は倒した敵です。対象や配置場所が不足した場合はその回の未達分を再試行します。次回の時刻になったら新しい復活枠に切り替えます。
- 敵個体のexperienceMultiplierを保存し、朝になっても出現時の倍率を保ちます。経験値の端数は保持し、再復活でも元の経験値に倍率を掛けます。睡眠時に朝へ復活する敵は通常経験値です。
- バッグの右端「⠿」をドラッグすると一覧順を変更します。閉じて確定するとプレイ画面のスキル順にも反映します。左のスキルアイコンは配置用、外すボタンの下は回転後の形の見本です。
