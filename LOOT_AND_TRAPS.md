# 共通スキル・ドロップ・宝箱・隠し罠の設定

## 1. モンスター間でスキルを共有する

`src/data/enemySkills.ts` のIDは能力名で統一しています。

- `stoneThrow`: 投石
- `lunge`: 飛びかかり

`src/data/enemies.ts` の好きな敵へ `skills: ['stoneThrow', 'lunge']` を設定できます。初期MPは同じ敵の `mp` で指定します。効果・発動条件・演出・音はスキル定義側にあり、使用者名による分岐はありません。旧セーブの `goblinStone` / `wolfLunge` は読み込み時に移行し、消費済みMPは維持します。

## 2. 敵ごとのドロップ表

`src/data/enemies.ts` の各敵に `drops` を設定します。

```ts
drops: [
  { loot: { type: 'item', id: 'potion' }, chance: 0.08 }, // 撃破ごとに8%
  { loot: { type: 'item', id: 'ether' }, chance: 0.03, count: 2 }, // 3%で2個
  { loot: { type: 'skill', id: 'fireball' }, chance: 0.02 }, // 2%
],
```

各行は独立抽選です。複数が同時に出ることも、何も出ないこともあります。確率の合計を100%に合わせる必要はありません。`drops: []` ならドロップなし。共通の12%抽選は廃止し、この表を直接使います。報酬は撃破位置の床へ置かれ、満杯の道具はその場に残ります。

## 3. 宝箱の中身

編集先は `src/data/loot.ts` の `CHESTS` です。下記は調整用の初期値です。

| 種類 | スキル | 道具 | 道具1個ごとのレア率 |
|---|---|---|---|
| 木 | なし | 必ず1〜2個 | 0% |
| 鉄 | 25%で1種類 | 必ず1〜2個 | 1% |
| 銀 | 必ず1種類 | 40%で1個 | 5% |
| 金 | 必ず異なる2〜3種類 | 90%で1〜2個 | 50% |

レア道具は遠見のレンズ・精霊の種、通常道具は回復薬・魔力の雫です。`src/data/items.ts` の `rarity` に分類を明記し、抽選候補は `COMMON_ITEMS` / `RARE_ITEMS` で指定します。道具を追加した場合は分類と抽選候補の両方へ登録してください。

- `chance`: 追加抽選の実確率（0〜1）
- `min` / `max`: 当選時の個数範囲
- `rareChance`: 道具1個ごとにレア候補から選ぶ確率
- `pool`: スキル候補と重み。重みは相対比で、合計100でなくて構いません。
- `color` / `trim`: 宝箱の本体色・金具色

手作りマップの `objects` には次のように追加します。

```ts
{ id: 'silver-01', type: 'chest', chestTier: 'silver', position: { x: 5, y: 6 } },
```

固定で入れたいスキルは `skillId` / `skillIds`、道具は `itemId` で追加できます。これらは最低保証であり、箱の個数まで不足分を抽選します。金の保証スキルも含め、同じ箱内では重複しないように選びます。完全に固定したい場合は `contents: [{ type: 'item', id: 'potion' }]` のように全内容を指定できます。この場合は箱の一般ルールより固定内容を優先します。

開始時の箱は金で、アタックとランダムワープを保証します。火雨の箱は銀です。箱の内容はマップ生成時（宝箱トラップでは箱の出現時）に確定し保存されるため、再開で変わりません。取得できない道具は床に残ります。旧セーブの箱は元の固定内容を維持します。

## 4. 隠し罠5種類

定義は `src/data/traps.ts` の `TRAPS`、実行処理は `src/game/TrapSystem.ts` です。

| ID | 名前 | 初期効果 |
|---|---|---|
| `treasure` | 宝箱トラップ | 周囲8マスの空き床に宝箱を1つ生成 |
| `bearTrap` | トラばさみ | 次の1行動、移動不可 |
| `fireMine` | 炎地雷 | 中心を含む3×3内に炎3ダメージ・炎付着 |
| `rockfall` | 落石 | 5×5内の床から毎波5マスを選び、自然1ダメージを3波 |
| `healing` | 回復 | HP5回復（上限あり） |

- 通常画面・ミニマップ・全体マップには罠を表示しません。
- プレイヤーが歩いて入った場合だけ一度発動します。敵・召喚味方の移動、待機、カメラ移動、ワープでは発動しません。
- トラばさみ中も攻撃・道具・待機は可能。方向入力で移動を試みると、その場で1行動消費して拘束が解けます。ワープは拘束中に使用できません。
- 炎地雷・落石は味方敵の区別なく命中します。大型キャラは占有セルのどこかが当たると、1波につき1回ダメージを受けます。壁マスは対象外で、爆発・落石は範囲内の壁越しの床にも届きます。
- 落石は各波内ではマスの重複なし。波ごとに再抽選します。床が5マス未満なら存在する床だけが対象です。
- 罠の環境ダメージを、プレイヤーが直接殴ったものとして敵視させる処理はしません。炎の属性反応は通常どおり起こります。
- 宝箱の等級比率は木60・鉄25・銀12・金3です。周囲が壁・キャラ・道具・未発動罠などで埋まっている場合は不発のログを表示します。
- 発動済み状態・拘束の残り・生成した箱・乱数状態はセーブします。

## 5. 全体配置と局所密集

地域別のステージファイル（例 `src/stages/plains/1-1.ts`）、またはその `layout` の手作りレイアウトに `trapPlacements` を追加します。レイアウトに指定した場合はステージ側より優先します。`[]` は罠なしです。

```ts
trapPlacements: [
  // 全体へ5個。宝箱と回復を同じ重みで抽選。
  { count: 5, pool: [
    { value: 'treasure', weight: 1 },
    { value: 'healing', weight: 1 },
  ] },
  // x=8〜12、y=9〜13の5×5へ、さらに4個密集させる。
  { region: { x: 8, y: 9, width: 5, height: 5 }, count: 4, pool: [
    { value: 'fireMine', weight: 3 },
    { value: 'bearTrap', weight: 1 },
  ] },
],
```

全体配置も局所配置も、壁・宝箱・道具・設置効果・開始地点・初期キャラ位置・既存罠を除外します。ルールは上から順番に適用し、配置範囲が重なっても同じマスへ重複配置しません。空きが足りない場合は設定エラーになります。

手作りレイアウトで固定位置に置く場合：

```ts
traps: [
  { id: 'snare-entry', trapId: 'bearTrap', position: { x: 4, y: 5 }, triggered: false },
],
```

平原1〜5には全体配置で6〜10個を設定しました。新しい冒険で反映します。既存セーブへ突然罠を追加することはありません。

## 6. 効果音・アニメーションを増やす

- `src/data/effects.ts`: 使用者に依存しない効果音キーと音程・波形・長さ。
- `src/render/EffectPainter.ts`: 罠のvisualキーに対応した描画。
- スキル・罠の定義で `visual` と `sound` を指定して再利用します。
- `GameEvent.delayMs` と `durationMs` で移動完了後の発動や3波のタイミングを設定します。

新しい数値違いの罠なら定義を追加し、必要ならTrapIdへIDを追加します。新しい種類の効果はeffectの型とTrapSystemの分岐、独自の絵が必要ならEffectPainterを追加します。

## 追加：狼の縄張り

`src/data/traps.ts` の `wolfTerritory` は `effect: { type: 'summonPerimeter', radius: 4, count: 3, enemy: 'wolf' }` です。踏んだ地点を中心とする9×9の外周から空きマスを重複なしで選び、森の狼を3体召喚します。壁・キャラ・宝箱などがある地点は避け、空きが3マス未満なら配置可能数だけ召喚してログで知らせます。新しい狼は通常の敵行動フェーズから動きます。

`enemy` を別の敵IDにすれば同じ処理を流用できます。範囲・個数はradius/countで調整します。演出はsummonRing、音はhowl。通常の罠抽選表にも重み1で追加済みです。既存マップの罠は再抽選しないため、新しい冒険から登場します。

アイスストーン（icestone）も宝箱の共通スキル抽選表に追加しました。既に生成済みの宝箱の中身は維持します。

地域共通・階層別の抽選表は [地域・階層設定ガイド](src/stages/README.md) を参照してください。poolを省略した罠配置は地域共通表を使います。
